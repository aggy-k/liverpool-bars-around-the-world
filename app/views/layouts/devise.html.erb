<!DOCTYPE html>
<html>
  <head>
    <title>LiverpoolBarsAroundTheWorld</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "hello", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <main class="container mx-auto mt-28 px-5">
      <div class="flex justify-center px-6 my-12 text-xs">
        <div class="w-full xl:w-3/4 lg:w-11/12 flex">
          <div class="w-full h-auto bg-gray-400 hidden lg:block lg:w-1/2 bg-cover rounded-l-lg" style="background-image: url(<%= asset_url('wp01.jpg') %>)"></div>
          <div class="w-full lg:w-1/2 bg-gray-50 p-5 rounded-lg lg:rounded-l-none">
            <div class="w-full flex justify-center mt-3">
              <!-- logo -->
            </div>
            <%= yield %>
          </div>


        </div>
      </div>
    </main>
  </body>
</html>
